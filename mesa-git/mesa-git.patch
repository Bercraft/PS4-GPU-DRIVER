diff -rupN mesa-master/include/pci_ids/radeonsi_pci_ids.h mesa-git/include/pci_ids/radeonsi_pci_ids.h
--- mesa-master/include/pci_ids/radeonsi_pci_ids.h	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/include/pci_ids/radeonsi_pci_ids.h	2020-05-31 17:48:29.762774200 +0200
@@ -145,6 +145,10 @@ CHIPSET(0x131B, KAVERI)
 CHIPSET(0x131C, KAVERI)
 CHIPSET(0x131D, KAVERI)
 
+CHIPSET(0x9920, LIVERPOOL)
+CHIPSET(0x9922, LIVERPOOL)
+CHIPSET(0x9923, LIVERPOOL)
+
 CHIPSET(0x67A0, HAWAII)
 CHIPSET(0x67A1, HAWAII)
 CHIPSET(0x67A2, HAWAII)
diff -rupN mesa-master/src/amd/addrlib/src/amdgpu_asic_addr.h mesa-git/src/amd/addrlib/src/amdgpu_asic_addr.h
--- mesa-master/src/amd/addrlib/src/amdgpu_asic_addr.h	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/amd/addrlib/src/amdgpu_asic_addr.h	2020-05-31 17:51:33.751593700 +0200
@@ -71,6 +71,9 @@
 
 #define AMDGPU_SPECTRE_RANGE    0x01, 0x41
 #define AMDGPU_SPOOKY_RANGE     0x41, 0x81
+#define AMDGPU_SPOOKY_RANGE     0x41, 0x61
+#define AMDGPU_STARSHA_RANGE    0x61, 0x71
+#define AMDGPU_STARSHP_RANGE    0x71, 0x81
 #define AMDGPU_KALINDI_RANGE    0x81, 0xA1
 #define AMDGPU_GODAVARI_RANGE   0xA1, 0xFF
 
@@ -115,6 +118,8 @@
 
 #define ASICREV_IS_SPECTRE(r)          ASICREV_IS(r, SPECTRE)
 #define ASICREV_IS_SPOOKY(r)           ASICREV_IS(r, SPOOKY)
+#define ASICREV_IS_STARSHA(r)           ASICREV_IS(r, STARSHA)
+#define ASICREV_IS_STARSHP(r)           ASICREV_IS(r, STARSHP)
 #define ASICREV_IS_KALINDI(r)          ASICREV_IS(r, KALINDI)
 #define ASICREV_IS_KALINDI_GODAVARI(r) ASICREV_IS(r, GODAVARI)
 
diff -rupN mesa-master/src/amd/common/ac_gpu_info.c mesa-git/src/amd/common/ac_gpu_info.c
--- mesa-master/src/amd/common/ac_gpu_info.c	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/amd/common/ac_gpu_info.c	2020-05-31 17:56:04.002886700 +0200
@@ -128,6 +128,8 @@ get_l2_cache_size(enum radeon_family fam
 		return 1024 * 1024;
 	case CHIP_FIJI:
 	case CHIP_POLARIS10:
+	case CHIP_LIVERPOOL:
+	case CHIP_GLADIUS:
 		return 2048 * 1024;
 		break;
 	default:
@@ -370,6 +372,8 @@ bool ac_query_gpu_info(int fd, void *dev
 	case FAMILY_KV:
 		identify_chip2(SPECTRE, KAVERI);
 		identify_chip2(SPOOKY, KAVERI);
+		identify_chip2(STARSHA, LIVERPOOL);
+		identify_chip2(STARSHP, GLADIUS);
 		identify_chip2(KALINDI, KABINI);
 		identify_chip2(GODAVARI, KABINI);
 		break;
@@ -506,7 +510,7 @@ bool ac_query_gpu_info(int fd, void *dev
 	info->pa_sc_tile_steering_override = device_info.pa_sc_tile_steering_override;
 	info->num_render_backends = amdinfo->rb_pipes;
 	/* The value returned by the kernel driver was wrong. */
-	if (info->family == CHIP_KAVERI)
+	if (info->family == CHIP_KAVERI || info->family == CHIP_LIVERPOOL)
 		info->num_render_backends = 2;
 
 	info->clock_crystal_freq = amdinfo->gpu_counter_freq;
@@ -1023,6 +1027,8 @@ ac_get_gs_table_depth(enum chip_class ch
 	case CHIP_PITCAIRN:
 	case CHIP_VERDE:
 	case CHIP_BONAIRE:
+	case CHIP_LIVERPOOL:
+	case CHIP_GLADIUS:
 	case CHIP_HAWAII:
 	case CHIP_TONGA:
 	case CHIP_FIJI:
@@ -1095,6 +1101,14 @@ ac_get_raster_config(struct radeon_info
 		raster_config = 0x3a00161a;
 		raster_config_1 = 0x0000002e;
 		break;
+	case CHIP_LIVERPOOL:
+		raster_config = 0x2a00161a;
+		raster_config_1 = 0x00000000;
+		break;
+	case CHIP_GLADIUS:
+		raster_config = 0x2a00161a;
+		raster_config_1 = 0x0000002e;
+		break;
 	default:
 		fprintf(stderr,
 			"ac: Unknown GPU, using 0 for raster_config\n");
diff -rupN mesa-master/src/amd/common/amd_family.h mesa-git/src/amd/common/amd_family.h
--- mesa-master/src/amd/common/amd_family.h	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/amd/common/amd_family.h	2020-05-31 17:56:35.347910100 +0200
@@ -81,6 +81,8 @@ enum radeon_family {
     CHIP_HAINAN,
     CHIP_BONAIRE,  /* GFX7 (Sea Islands) */
     CHIP_KAVERI,
+	CHIP_LIVERPOOL,
+    CHIP_GLADIUS,
     CHIP_KABINI,
     CHIP_HAWAII,
     CHIP_TONGA,    /* GFX8 (Volcanic Islands & Polaris) */
diff -rupN mesa-master/src/amd/llvm/ac_llvm_util.c mesa-git/src/amd/llvm/ac_llvm_util.c
--- mesa-master/src/amd/llvm/ac_llvm_util.c	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/amd/llvm/ac_llvm_util.c	2020-05-31 17:57:57.285354100 +0200
@@ -115,6 +115,8 @@ const char *ac_get_llvm_processor_name(e
 	case CHIP_HAINAN:
 		return "hainan";
 	case CHIP_BONAIRE:
+	case CHIP_LIVERPOOL:
+	case CHIP_GLADIUS:
 		return "bonaire";
 	case CHIP_KABINI:
 		return "kabini";
diff -rupN mesa-master/src/amd/vulkan/winsys/null/radv_null_winsys.c mesa-git/src/amd/vulkan/winsys/null/radv_null_winsys.c
--- mesa-master/src/amd/vulkan/winsys/null/radv_null_winsys.c	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/amd/vulkan/winsys/null/radv_null_winsys.c	2020-06-01 09:49:56.938403900 +0200
@@ -42,6 +42,10 @@ static const struct {
 	[CHIP_OLAND] = { 0x6060, 2 },
 	[CHIP_HAINAN] = { 0x6660, 2 },
 	[CHIP_BONAIRE] = { 0x6640, 4 },
+	[CHIP_LIVERPOOL] = { 0x9920, 8 },
+	[CHIP_LIVERPOOL] = { 0x9922, 8 },
+	[CHIP_LIVERPOOL] = { 0x9923, 8 },
+	[CHIP_GLADIUS] = { 0x9924, 16 },
 	[CHIP_KAVERI] = { 0x1304, 2 },
 	[CHIP_KABINI] = { 0x9830, 2 },
 	[CHIP_HAWAII] = { 0x67A0, 16 },
diff -rupN mesa-master/src/gallium/drivers/radeonsi/si_state_draw.c mesa-git/src/gallium/drivers/radeonsi/si_state_draw.c
--- mesa-master/src/gallium/drivers/radeonsi/si_state_draw.c	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/gallium/drivers/radeonsi/si_state_draw.c	2020-06-01 09:51:32.539852100 +0200
@@ -336,8 +336,9 @@ static unsigned si_get_init_multi_vgt_pa
 
       /* Bug with tessellation and GS on Bonaire and older 2 SE chips. */
       if ((sscreen->info.family == CHIP_TAHITI || sscreen->info.family == CHIP_PITCAIRN ||
-           sscreen->info.family == CHIP_BONAIRE) &&
-          key->u.uses_gs)
+           sscreen->info.family == CHIP_BONAIRE ||
+           sscreen->info.family == CHIP_LIVERPOOL) /* ? */ &&
+		   key->u.uses_gs)
          partial_vs_wave = true;
 
       /* Needed for 028B6C_DISTRIBUTION_MODE != 0. (implies >= GFX8) */
diff -rupN mesa-master/src/gallium/targets/d3dadapter9/description.c mesa-git/src/gallium/targets/d3dadapter9/description.c
--- mesa-master/src/gallium/targets/d3dadapter9/description.c	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/gallium/targets/d3dadapter9/description.c	2020-06-01 09:52:27.596135000 +0200
@@ -53,6 +53,8 @@ cards_amd[] = {
     {"KAVERI",                      "AMD Radeon(TM) R7 Graphics"},
     {"KABINI",                      "AMD Radeon HD 8400 / R3 Series"},
     {"BONAIRE",                     "AMD Radeon HD 8770"},
+	 {"LIVERPOOL",                   "AMD Liverpool (Playstation 4)"},
+    {"GLADIUS",                     "AMD Gladius (Playstation 4 Pro)"},
     {"OLAND",                       "AMD Radeon HD 8670"},
     {"HAINAN",                      "AMD Radeon HD 8600M Series"},
     {"TAHITI",                      "AMD Radeon HD 7900 Series"},
diff -rupN mesa-master/src/gallium/winsys/radeon/drm/radeon_drm_winsys.c mesa-git/src/gallium/winsys/radeon/drm/radeon_drm_winsys.c
--- mesa-master/src/gallium/winsys/radeon/drm/radeon_drm_winsys.c	2020-05-31 02:24:58.000000000 +0200
+++ mesa-git/src/gallium/winsys/radeon/drm/radeon_drm_winsys.c	2020-06-01 09:54:30.950022500 +0200
@@ -274,6 +274,8 @@ static bool do_winsys_init(struct radeon
       ws->info.chip_class = GFX6;
       break;
    case CHIP_BONAIRE:
+   case CHIP_LIVERPOOL:
+   case CHIP_GLADIUS:
    case CHIP_KAVERI:
    case CHIP_KABINI:
    case CHIP_HAWAII:
@@ -492,6 +494,8 @@ static bool do_winsys_init(struct radeon
    case CHIP_VERDE:
    case CHIP_OLAND:
    case CHIP_BONAIRE:
+   case CHIP_LIVERPOOL:
+   case CHIP_GLADIUS:
    case CHIP_KAVERI:
       ws->info.num_tcc_blocks = 4;
       break;
@@ -521,9 +525,11 @@ static bool do_winsys_init(struct radeon
       case CHIP_TAHITI:
       case CHIP_PITCAIRN:
       case CHIP_BONAIRE:
+	  case CHIP_LIVERPOOL:
          ws->info.max_se = 2;
          break;
       case CHIP_HAWAII:
+	  case CHIP_GLADIUS:
          ws->info.max_se = 4;
          break;
       }
